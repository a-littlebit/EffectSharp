<Window x:Class="Example.Wpf.Todos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button" x:Key="SelectButtonStyle">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
        <Style TargetType="Button" x:Key="SelectAllButtonStyle" BasedOn="{StaticResource SelectButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowAllItems.Value}" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="SelectPendingButtonStyle" BasedOn="{StaticResource SelectButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowPendingItems.Value}" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="SelectCompletedButtonStyle" BasedOn="{StaticResource SelectButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowCompletedItems.Value}" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Todo" HorizontalAlignment="Left" FontSize="48" Margin="340,20,0,20" />

        <Grid Grid.Row="1" Margin="10,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding NewTodoTitle.Value, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Height="30" Padding="3">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding AddTodoCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <Button x:Name="AddButton" Grid.Column="1" Content="Add" Margin="10,0,0,0" Padding="5" Command="{Binding AddTodoCommand}" />
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Height="30" Margin="10">
            <Button Style="{StaticResource SelectPendingButtonStyle}" Command="{Binding SelectPendingCommand}">Pending</Button>
            <Button Style="{StaticResource SelectCompletedButtonStyle}" Command="{Binding SelectCompletedCommand}">Completed</Button>
            <Button Style="{StaticResource SelectAllButtonStyle}" Command="{Binding SelectAllCommand}">All</Button>
        </StackPanel>

        <ScrollViewer Grid.Row="3" Margin="20,20,20,20" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical">
                <ItemsControl ItemsSource="{Binding FilteredTodoItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <CheckBox IsChecked="{Binding IsCompleted}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding Title}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
